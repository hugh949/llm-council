/** App version (x.y: major.minor) - bump minor for releases, major for breaking changes */
export const APP_VERSION = '1.0';

/** Build timestamp (set at build time by Vite) - helps verify deployed version */
export const BUILD_TIME = __BUILD_TIME__;
